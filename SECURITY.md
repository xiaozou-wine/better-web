# 安全策略

## 报告漏洞

请通过 GitHub 的 **Security → Report a vulnerability** 私下提交，不要开公开 issue。

这个项目处理代理凭据和浏览器指纹，以下类型的问题请优先报告：

- 代理密码泄漏到日志、错误信息、导出文件或前端响应
- 指纹一致性缺陷：同一 profile 两次启动指纹不同，或各维度互相矛盾
- 内核下载安装环节的路径穿越、校验绕过
- 本地转发器的认证绕过或流量串号

## 已知边界

以下是设计上的限制，不算漏洞：

- **非 Windows 平台的凭据以明文存储。** `internal/secret` 只在 Windows 上走 DPAPI，
  其他平台如实报告为明文，不假装加密。
- **本地转发器监听回环地址且不做认证。** 同一台机器上的其他进程可以经由它访问代理。
  这是补齐内核 `--proxy-server` 不支持密码认证的必要代价。
- **不防御本机上的攻击者。** SQLite 库和 user-data-dir 依赖操作系统的文件权限。
  取得你账号权限的人可以读到一切。
- **指纹伪造不保证不被识别。** 商业风控在持续演进，README 的实测数据有明确的
  时间与版本边界，不构成对未来版本的承诺。
